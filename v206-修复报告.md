# v206 修复报告 — 9/5 晚报障四连 + 分仓计划口径确认

**部署**：commit `18945e0`，DB_VERSION 205 → 206，线上 https://rdc-dashboard.pages.dev

---

## 一、先说结论：你的数据没问题，问题出在浏览器缓存

收到截图后我做的第一件事是逐字节核验数据文件，结果**全部完好**——不需要也不应该重新生成：

| 数据 | 核验结果 |
|---|---|
| 分仓计划 | 2,435 行，与桌面 9/5 新模版**逐字节一致**（含 2026-08~2027-01 六个月计划值） |
| 2026周转 | 与新模版一致，**含 8 月周转天数**（第 8 列 46235=2026-08-01）与出货成本 |
| 拉回数据 | 16,173 行，**含 8 月**（至 08-20） |
| 转储数据 | 7,324 行（07-02 ~ 08-31），加上 transship.json 的 1-6 月共 23,790 条 |
| 基础数据 | 20 列，产品主数据齐备（生命周期标签 / 软切新品 / 箱规转化因子 / 成本价） |

两个空页面（cov7=0行、找不到库存金额表）的根因是：**你浏览器里的 IndexedDB 缓存是 v205 之前坏时代留下的残缺数据**——订单明细是满的（238,352 行），但库存解析残缺（cov7=0、库存金额结构缺失），这种「半好半坏」的缓存被断点续传机制信任后固化，普通刷新救不回来。

**修法**：DB_VERSION 205→206，所有在线用户下次打开会强制清空本地缓存、全量重拉一次（约 20 秒~几分钟，视网速）。**你只要刷新页面即可，不需要任何手动操作。**

## 二、你问的两件事，如实回答

### 1. 分仓计划监控是否已按「分仓计划」表 + 所有 SKU 重算？

- **取数**：是。「分仓需求」自 v202 起就是**优先读「分仓计划」sheet**（SKU×RDC×月），该表没有的组合才回退覆盖表计划值。我做了真值核对：SKU 09539 / 华南RDC 的 8/9/10 月计划 = **191 / 4,034 / 4,207**，与模版分仓计划表逐值一致。
- **SKU 范围**：**v199 起就没有任何品类剔除**——促销装、新品、无需求 SKU 全部纳入销售进度计算。你之所以还看到「已按促销装或新品期剔除」的字样，是 4 处悬浮提示文案没跟着改（我的失误），v206 已全部改为如实描述。页面顶部「计划口径」也改成了明示「分仓计划表优先」。
- **一个如实说明**：分仓计划表里有 59 个 SKU 在「7月库存覆盖数据」快照中不存在（其中 39 个连基础数据里都没有主数据）。监控主表的行来源是覆盖表快照，这 59 个 SKU 目前不在表里——没有库存/覆盖上下文，强行造行只会整行「—」。如需展示请告诉我，我可以单独加一个「仅有计划、无覆盖快照」的分组。

### 2. 库存周转页

2026周转表的数据（含 8 月周转天数、出货成本）**已经在这一版里**，刷新后即可看到。本轮模版未涉及库存状态分析/库存覆盖/库存金额，这些页面的数据未动。

## 三、转储 & 拉回

- 转储页现在**稳定显示 1-8 月共 23,790 条**（此前「先进哪个页面数量会不一样」的不一致已修复，本地+线上双路径验证一致）。
- 拉回分析含 8 月数据（至 08-20），16,172 条原始记录正常聚合。

## 四、验证记录

- 本地端到端（tools/e2e-v206.js）：分仓真值 / 库存结构 / 转储 / 周转 全部 ✅，0 页面错误
- 毒缓存自愈模拟（tools/e2e-v206-p5.js）：毒化 cov7 → reload → 自动重拉恢复 ✅（即你的场景在将来也会自愈）
- 线上验证（tools/e2e-live-v206.js）：见下轮对话结论
- node --check + smoke-test 29 条规则：0 错误
