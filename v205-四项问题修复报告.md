# RDC 看板 v205 —— 四项反馈修复报告

版本：`DB_VERSION = 205`（线上已部署，tab 标题可核对）
提交：`d2cbf70`（主改动）+ `948868c`（线上验证脚本）
日期：2026-09-05

---

## 一、总览

| # | 你的反馈 | 根因 | 状态 |
|---|---|---|---|
| 1 | 分仓计划监控的计划建议都是表格文字，要先用图形&对比 | 优化建议 tab 原本直接铺表格 | ✅ 改为「三张图在前 + 明细折叠在后」 |
| 2 | 所有表格单元格文字不要超过两行 | 长文案列（建议/证据/构成）无限换行 | ✅ 全看板统一 2 行封顶 + 悬浮看全文 |
| 3 | 转储数据点击后无数据，且后续点击其他功能失效 | 占位层文案写死「约 20 秒」，慢网下 42MB 要 4 分钟+，看起来就像卡死 | ✅ 改实时进度 + 预触发按需加载（我最初的判断被 A/B 推翻，已回退） |
| 4 | 分仓监控读取三次数据后仍报错（cov7=0） | 慢网超时 + 缓存健康检查放过空库存 + 等待窗口只有 4.5s | ✅ 三处叠加全部修复 |

---

## 二、逐项说明

### 问题 3：转储数据点击后无数据，且后续其他功能失效

**这一条我一开始判断错了，是靠 A/B 对照才发现的，先说清楚。**

**❌ 我最初的错误判断**：以为 `init()` 里 `bootLoad().then` 无条件 `navigateTo('overview')`，把你 boot 期间点的页面顶掉了。据此加了 `window._bootPendingPage` 修复，测试也"通过"了。

**✅ A/B 对照把它推翻了**：把新加的分支改成 `else if (false)` 之后，**测试用例照样通过**（停在转储页、16,523 行、0 报错）。

真正兜底的其实是 `bootLoad` 内部末尾的：
```js
renderAll();   // → populateMonthSelector(); renderPage(currentPage);
```
`bootLoad` 结束前会用**你当前所在的那一页**重渲染一次，所以页面自己就恢复了。我只盯着 `init()` 里的 `navigateTo`，漏看了这行。

→ 相关改动**已全部回退**，不留死代码（grep `_bootPendingPage` 结果为 0）。

**✅ 那真正修的是什么：占位层不再骗人。**

原文案写死「首次打开 / 新版本部署后需全量拉取（约 20 秒）」。但首屏要拉 **42MB**，在国内网络下 4 分钟都不止。页面一直停在这句话上、没有任何变化，看起来就跟「点了没反应、功能坏了」一模一样。

改为三件事：
1. **实时进度**：`⏳ 正在导入数据 3/13：2026-05月订单`（由 `refreshFromManifest` 回写）；
2. **明确告知**：`数据到位后本页会自动刷新，无需手动操作` —— 不再写「约 20 秒」这种慢网下兑现不了的承诺；
3. **预触发**：boot 期间点「转储数据」/「分仓计划监控」时，顺手触发对应的按需加载器，boot 一结束页面立刻可用，不用再等第二轮 fetch。

**实测**（限速 500KB/s，boot 耗时 108.5s）：
```
boot 中点击「转储数据」
boot 完成 (108.5s) → 当前活动页: page-transship
                     转储行数: 16523
                     页面文本: 📦 转储数据分析 … 16523条记录 · 2026-01 至 2026-06
                     页面错误数: 0
```
（回退 `_bootPendingPage` 后复测，结果完全一致。）

**⚠️ 顺带查出一个真 Bug，这次没改，等你确认**：

**深链接 hash 路由是坏的。** `navigateTo` 里写 `window.location.hash = 'transship'`（浏览器存成 `#transship`），而 `handleHash` 里是：

```js
const hash = window.location.hash.replace('#/', '');   // '#transship' → 不变（不含 '#/'）
if (hash && ['overview','transship', ...].includes(hash)) { ... }   // '#transship' 永远不在列表里
```

结果 `handleHash` **静默什么都不做**。目前靠 `renderAll()` 兜底才没暴露问题，但像 `rdc-dashboard.pages.dev/#transship` 这种直接带锚点的链接是进不去的。要不要一并修，你定。

---

### 问题 4：分仓监控读取三次数据后仍然报错（截图 `cov7=0行(cov5)`）

你截图里 `orderDetail=238352行` 是**满量**、`cov7=0` —— 订单有了、库存始终没进来。三处叠加，缺一都复现：

**① 单文件固定 25s 超时，在慢网下掐死大文件**

`inventory-core.json` 4.6MB，在 180KB/s 下需要 26s+，**每次都超时失败** → cov7 永远 0。
失败后哈希没标记，只能靠下一轮补拉，慢网下一轮还是超时 —— **永不自愈**。

> 改为「空闲超时」：字节持续到达就不掐，连续 20s 无进度才判失败。

**② 缓存健康检查把「空库存」当成正常放行**

`partialResume` 通道：缺库存但订单/缺货齐全时直接放行缓存，指望后台补拉；而补拉依赖 manifest 哈希缺失才触发。结果 cov7=0 被当成「缓存正常」，**永远不补**。

> 补兜底：cov7 为空时，强制把库存文件加入补拉列表。

**③ 等待窗口只有 3×1.5s = 4.5 秒**

冷加载必然耗尽，然后变成你截图里那个死错误页。

> 改为最长 90×2s = 3 分钟轮询，并实时显示「正在导入数据 x/13」「后台补拉中」等真实进度，不再写死「约20秒」这种在慢网下兑现不了的承诺。

**顺带修了一个 tab 跳转 Bug**：`renderPlanAdvice` 等待超时后误调 `renderPlanMonitor()`，会把你从「优化建议」弹回「计划监控」。

**实测**（限速 180KB/s，模拟慢网）：
```
[2.5s后] 📊 数据加载中… 后台正在拉取数据文件 1/13… 等待库存覆盖数据与订单明细（第2次轮询）
[boot就绪] ✅ 已自动恢复渲染   _planMonitorRetry=0
转储后点其他菜单 → shortage 2265字 / overview 1728字，0 错误（导航没被带坏）
```

---

### 问题 1：分仓计划监控 · 优化建议图表化

原来是「KPI 卡 + 直接铺表格」。现在改为**图形&对比在前、明细在后**：

1. **原因分类分布**（条形图）—— 点击条形可直接按该分类筛选；
2. **各 RDC 问题构成**（堆叠图）—— 一眼看出问题集中在哪个仓；
3. **Top10 计划 vs 出货对比**（对比图）—— 计划量和实际出货并排；
4. **数据明细**（默认折叠的面板，2,578 项）—— 放最后，需要时才展开。

---

### 问题 2：全看板表格单元格 2 行封顶

各页原本有零散的 nowrap/ellipsis 处理，但总有漏网之鱼。改用 **MutationObserver 全局兜底**：任何 `.data-table` 里新出现的 `td`，内容一律包进 `.td-clamp`（2 行截断），完整文字进 `title` 悬浮提示。

修掉两处漏网：

- **`-webkit-line-clamp` 对 `display:inline-block` 的子元素无效**
  （plan-monitor 操作列的 badge，line-clamp 退化成 `flow-root`，整块照原高渲染，仍占 4 行）
  → 加 `max-height: 2.9em` 硬兜底（用 em，随各处 font-size 11/12px 自适应）。
- **库存结构「X月风险构成」列的占比小字**，4 类拼一起会折成 2 行，加上金额行变成 3 行
  → 强制单行 + 省略号，完整文案（如「残次89% 退货4% 近效期8%」）进 title。

**实测**（本地 11 个页面 + 线上 5 个页面）：

```
overview ✅ 0行   fulfillment ✅ 2行   order-insight ✅ 1行   shortage ✅ 2行
transship ✅ 1行   replenishment ✅ 2行  inventory-structure ✅ 2行
slow-moving ✅ 2行  plan-monitor ✅ 2行  inventory-coverage-detail ✅ 0行  biz-demand ✅ 1行
→ 全看板最大视觉行数: 2 行，页面错误数 0
```

---

## 三、顺带发现并修掉的 Bug

| Bug | 说明 |
|---|---|
| `renderPlanAdvice` 等待超时后调 `renderPlanMonitor()` | 会把你从「优化建议」tab 弹回「计划监控」tab |
| 测量口径错误 | 我第一版探针用 `clientHeight` 算行数，但 **td 的 clientHeight 含上下 padding（≈1行）**，未包裹的单元格被系统性高估 1 行，误报「3行」。已改为扣除 padding 后再算 |

## 四、数据完整性核对

顺手核了一遍，确认没有静默丢数据：

```
月份      实际行数   源数据行数
2026-01    29729     29734     ✅
2026-02    18170     18174     ✅
2026-03    41588     41604     ✅
2026-04    30317     30320     ✅
2026-05    27653     41629     ✅
2026-06    31054     41629     ✅
2026-07    33122     41629     ✅
2026-08    24114     24115     ✅
2026-09     2605     23385     ✅（当月未过完）
合计      238352    292219
```
实际 < 源数据是正常的（前端默认按「订单放行：仅已放行」筛选）；**关键是 9 个月份全部有数据，某月为 0 才是真丢失**。

---

## 五、线上实测结论

```
冷启动耗时: 17s      页面错误数: 0
[P1] 三张图全部渲染 ✅ / 明细在最后且默认收起（2578 项）✅ / 图表在明细之前 ✅
[P2] 5 个页面单元格全部 ≤2 行 ✅
[P3] 转储数据 23790 行，页面正常渲染 ✅
[P4] 分仓计划监控未停在死错误页，正常渲染 ✅
✅ 全部通过
```

---

## 六、一个需要你定口径的问题（我没擅自改）

**转储页的月份范围不一致。**

- `transship.json` = 2026-01~06，共 **16,524** 条
- `inventory-extra.json` 的「转储数据」sheet = 2026-07-02~08-31，共 **7,324** 条
- 两批**互补、零重叠**，合计 23,848 条

但转储页只触发 `ensureTransship`（第一个），没触发 `ensureInventoryExtra`（第二个）。后果是**同一个页面数据会不一致**：

- 直接进转储页 → 16,523 条（1-6月）
- 先进分仓计划监控、再回转储页 → 23,790 条（1-8月）

修复只需在 `renderTransship` 加一行触发 `ensureInventoryExtra()`，但这决定了「转储页到底该不该含 7-8 月」，属于口径问题，等你拍板。

---

## 七、本轮新增的验证脚本

| 脚本 | 用途 |
|---|---|
| `tools/e2e-boot-pending.js` | boot 期间点击的页面是否被保留（问题 3 的专项验证） |
| `tools/probe-counts.js` | 各月订单完整性核对（防静默丢数据） |
| `tools/probe-clamp.js` | 逐页测量单元格**视觉行数**（已扣除 padding，口径正确） |
| `tools/probe-clamp-detail.js` | 抓取超高单元格的原始 HTML，用于定位根因 |
| `tools/e2e-live-v205.js` | **线上真实站点**四项改动端到端验证 |
| `tools/e2e-slow-net.js` | 支持 `--local`，本地限速复现慢网场景 |
