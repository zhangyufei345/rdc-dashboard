# v202 数据导入提速报告（2026-09-05）

线上：https://rdc-dashboard.pages.dev ｜ commit `f8bde51` ｜ DB_VERSION 202

---

## 一、根因：和预想不一样

### 「每次更新都慢」的真凶 —— 不是文件太大，是无条件清空缓存

v188 起的逻辑：

```js
if (flag !== manifest.generatedAt) {
  localStorage.removeItem('rdc_manifest_hashes');  // 清掉「本地存了哪版数据」
  await clearIDB();                                 // 清空 25MB IndexedDB
}
```

**任何一次部署**都会清空 25MB 本地缓存并重拉全部 **42MB**，
哪怕本次只更新了库存（3.3MB）。这才是体感「每次更新都慢」的根源。

首屏 41.3MB 的构成：

| 类别 | 大小 | 说明 |
|---|---|---|
| 8 个月订单数据（data.json + data-2026-01~08.json） | **38MB** | 真正的体量主体 |
| inventory-core + inventory-master | 3.3MB | 库存 |
| adjustments + history | 0.1MB | |

### 「最近每次更新功能都出错」

v187 → v200 连续 6 个版本，每个都是「修 A 坏 B」，没有跨页回归机制。

---

## 二、修复

### 1. D 阶段（核心提速）：部署不再无条件清空缓存

- `manifest.generatedAt` 变化 → 只更新标记
- 只有 `DB_VERSION` 真的变（数据结构升级）→ 才 `clearIDB()`

🔑 **最关键的细节**（第一版踩坑）：`removeItem('rdc_manifest_hashes')` 必须移进
`if (needClear)` 分支内。这个 key 记录「IDB 里存了哪版数据」，是判断「哪些文件要重拉」的
唯一依据 —— 留在分支外，所有文件都会被判为已变更，仍然全量重拉，提速完全失效。

**效果：只更新库存 → 重拉 3.3MB（原 42MB）**

安全性不变：既有兜底健康检查（cov7<100 / orderDetail<10万）仍会强制全量重拉自愈。

### 2. A 阶段：inventory 五路拆分

| 输出文件 | 内容 | 大小 | 加载时机 |
|---|---|---|---|
| inventory-core.json | 订单满足率/周转/库存金额/库存覆盖/7月库存覆盖数据 | 0.64MB | 首屏 |
| inventory-master.json | 基础数据（产品主数据源） | 2.67MB | 首屏 |
| inventory-plan.json | 分仓计划 | 0.20MB | 按需 |
| inventory-extra.json | 拉回数据/转储数据/5·6月库存覆盖 | 4.27MB | 按需 |
| inventory-status.json | 5/6/7月库存状态分析 | 8.38MB | 按需 |

### 3. 🔴 修复 v201 的静默数据丢失（本次最严重）

v201 首版拆分只定义了 CORE/MASTER/PLAN，把 **拉回数据 / 转储数据 / 5·6月库存覆盖 /
状态分析** 落进「未分类」警告后**直接丢弃，没有任何报错**。

其中 **转储数据（2026-07~08，7323 行）与 transship.json（2026-01~06，16524 行）互补且零重叠**
→ 拆分后转储页的 7~8 月数据会彻底消失。

### 4. 🔴 补 `ensureInventoryPlan`

`inventory-plan.json` 拆出来后**没有任何加载器**，而它是 v198 指定的「分仓需求」权威源
（`getPlanDemand` 优先读它，缺则回退 cov7）。缺加载器 = `planBySkuRdc` 恒空 =
**分仓需求静默退回旧口径，用户完全无感**。

已在 `renderPlanMonitor` / `renderPlanAdvice` 进入时触发。

### 5. v198 部署漏拷：项目 inventory.xlsx 未随模板更新

| sheet | 老文件(8/12) | 新模板(9/5) |
|---|---|---|
| 分仓计划 | 无 | **2435 行（新增）** |
| 拉回数据 | 14410 | 16173 |
| 转储数据 | 3659 | 7324 |
| 基础数据 | 10628 | 10644（新增 block C 产品主数据列） |

旧文件已备份为 `inventory_20260812_before_v202.xlsx`。

**口径验证**：分仓计划 vs cov7 的 2026-08 计划量 **1847 组完全一致**，
另有 587 组是计划表独有（cov7 没有这些 SKU×RDC 组合）→ 切换到新口径安全。

### 6. 修 generate_data_json.mjs 的 TDZ

`const manifest` 声明在 for 循环之后；不带 `--only` 时循环体不执行所以不报错，
一用 `--only=orders` 就 `ReferenceError: Cannot access 'manifest' before initialization`。

---

## 三、回归防护：smoke-test 新增 R16-R20

| 规则 | 防什么 |
|---|---|
| R16 | inventory.xlsx 每个 sheet 必须落入某个输出 JSON（防静默丢失） |
| R17 | 按需文件必须有对应加载器（防「拆出但无人加载」—— v202 的坑） |
| R18 | `ensureInventoryPlan` 必须在 renderPlanMonitor + renderPlanAdvice 触发 |
| R19 | bootLoad 的 clearIDB 必须受 DB_VERSION 保护 |
| R20 | `removeItem('rdc_manifest_hashes')` 只能在 `if(needClear)` 分支内 |

> 检测器自身也会误判：R19 第一版按裸名 `clearIDB(` 匹配，把**注释正文**里引用的旧代码
> 当成了真实调用；改为只认 `await clearIDB(` 才准。

---

## 四、验证

- smoke-test：**61 项通过，0 错误**（17 个既有启发式警告，与本次改动无关）
- 线上 `DB_VERSION = 202`，6 个数据文件全部 HTTP 200 可访问
- `index.html` 与 `rdc-dashboard.html` 一致

---

## 五、下一轮提速方向

1. **订单 38MB 分片存 IDB** —— 首屏 41.3MB 的主体，按月分片后增量更新可到 MB 级
2. **`inventory-master.json` 列投影** —— 2.67MB 中约 1.6MB 是 30 列里的 `null` 占位
   （10644 × 30 × `"null"`），按前端实际查的 17 列投影可压到 ~1MB
