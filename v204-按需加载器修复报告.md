# v204 修复报告：三个按需加载器恒失败 + 转储跨部署丢失

> 部署时间：2026-09-05 · commit `0d6ba84` · DB_VERSION 203 → 204 · 线上已验证通过

---

## 一、最关键的发现：三个按需加载器半年多来 100% 抛异常

### 根因

`ensureTransship` / `ensureInventoryExtra` / `ensureInventoryPlan` 三处 fetch 都写成：

```js
signal: AbortController.timeout(30000).signal   // ❌ 这个 API 不存在
```

**Web 标准里没有 `AbortController.timeout()` 这个静态方法。** 超时信号的正确写法是
`AbortSignal.timeout(ms)`——静态方法挂在 `AbortSignal` 上，且直接返回 signal 本身。

浏览器实测（Chrome 152）：

```
AbortController.timeout → undefined
AbortSignal.timeout     → function ✅
```

后果是三行代码每次执行**同步抛 `TypeError`**，而三个函数都套了 `try/catch`，
异常被吞掉后表现为「数据永远加载不出来」而不是报错——控制台只有一条容易被忽略的 warn。

### 引入时间线（git 考古）

| 提交 | 日期 | 动作 |
|---|---|---|
| `3ce2b75` v198-fix3 | 2026-09-05 | 首次写入这个错误 API |
| `f8f9be2` v198-fix5 | 2026-09-05 | 复制到 ensureTransship |
| `f8bde51` v202 | 2026-09-05 | 复制到 ensureInventoryPlan |

### 影响面

| 加载器 | 依赖它的功能 | 修复前 |
|---|---|---|
| `ensureTransship` | 转储数据（1-6 月，16,523 行） | 转储页永远显示「加载失败」 |
| `ensureInventoryExtra` | 拉回数据 16,172 行 + 5/6 月库存覆盖 + 转储数据（7-8 月，7,324 行） | 批次拉回测算 / 拉回分析全空 |
| `ensureInventoryPlan` | 分仓计划 sheet（417 SKU） | **分仓需求恒空**，静默回退旧口径 cov7 |

**这与你反馈的「库存、转储和分仓需求相关模块」完全对应。**

需要说明：v199 修的「转储数据点了页面就死掉」只是把无限重试循环改成显示失败提示，
**根因一直没动**——恰恰是 fetch 抛异常导致 `ensureTransship` 永远失败，才触发了重试循环。

### 顺带暴露的第二个 Bug

修好超时信号后，立刻暴露下一个潜伏问题：

```
ReferenceError: applyPreparsed is not defined
```

`applyPreparsed` 是 `init()` 里定义的**嵌套闭包**，而三个加载器都是顶层函数，
作用域里根本看不见它（此前 fetch 先抛错，永远走不到这一行）。
已按 v113/v114 铁律补上 `window.applyPreparsed = applyPreparsed;`。

这与 v182 引入的 `pullbackDiag` 是**同一类 Bug**：顶层函数引用了看不见的标识符。
两次都是「node --check 查不出、控制台只是一条 warn、现象是数据静默为空」。

---

## 二、转储数据跨部署丢失

`parseExcel` 的 `replace` 模式此前**无条件**清空 `dataStore.transship`。
但实测 `data.json` / `data-2026-*.json` **都没有转储 sheet**（转储有两个独立来源：
`transship.json` 的 1-6 月 + `inventory-extra.json`「转储数据」sheet 的 7-8 月）。
结果是每次更新当月订单，转储数据全丢，必须重新点进转储页才恢复。

改为仅当本次文件真的含转储 sheet 时才清空（手工上传含转储的 xlsx 仍按原逻辑重置）。

### A/B 实测（同一套流程，只回滚这一行）

| 版本 | 部署前转储行数 | 部署后 | 结论 |
|---|---|---|---|
| 对照组（修复前） | 23,790 | **0** | 如期丢失 ✅ 证明测试有效 |
| 正式版（修复后） | 23,790 | **23,790** | 完整保留 ✅ |

---

## 三、体积优化：基础数据列投影

`generate_data_json.mjs` 新增 `projectEmptyColumns()`：删除「表头为空且整列无数据」的占位列。

- 基础数据 30 列 → **20 列**
- `inventory-master.json` **2.67MB → 2.17MB**（-19%）

安全性：前端用 `bColIdx()` 按**表头名**寻列，未命名列可安全删除；
硬编码的 `r[0]`/`r[1]`（产品编码/产品名称）位置未变。已加 R27 规则守着。

---

## 四、性能实测

| 指标 | 数值 |
|---|---|
| 线上冷启动（DB_VERSION 变更触发全量重拉） | **32s** |
| 从缓存打开（含 22MB 按需数据） | **~750ms** |
| 转储页渲染（23,790 行） | **118~180ms** |
| saveToIDB | 300ms（原 188ms，因多存了按需数据） |
| inventory 序列化体积 | 4.6MB → **15.1MB** |

结论：按需数据入库后，缓存打开仍是亚秒级，不需要再做分片。

---

## 五、回归防护：新增 3 条 smoke-test 规则

| 规则 | 作用 |
|---|---|
| **R27** | 基础数据必须含前端依赖的 14 列，且 `r[0]`/`r[1]` 仍为产品编码/产品名称（防列投影误删） |
| **R28** | 禁止 `AbortController.timeout` 等不存在的 API（注释感知，不会误报修复说明里的文字） |
| **R29** | 顶层函数不得越界调用 `init()` 的嵌套函数（applyPreparsed 类 Bug 通用兜底） |

R29 已做反向验证：临时拆掉 `window.applyPreparsed` 暴露点 → 立刻报 3 条错；还原 → 全绿。

新增两个 E2E 脚本：
- `tools/e2e-p4-transship.js`（含 `--control` 对照组 + 稳定判定）
- `tools/e2e-pages-sweep.js`（15 个页面全量回归）
- `tools/e2e-live.js`（**线上真实站点**端到端验证）

---

## 六、线上验证结果

```
线上 DB_VERSION = 204
首屏就绪: true  耗时 32s
按需加载器: {"transship":23790,"pullback":16172,"planSku":417}   加载期间错误: 0
逐页渲染: transship ✅ / slow-moving ✅ / plan-monitor ✅ / inventory-structure ✅
总错误数: 0 | 异常页面: 0
```

15 个页面全量回归：**0 报错**。

---

## 七、发现但**未改动**的问题（等你确认口径）

**转储页只显示 1-6 月，缺 7-8 月。**

转储页只触发 `ensureTransship`（transship.json，1-6 月，16,523 条），
没有触发 `ensureInventoryExtra`——而 `inventory-extra.json` 里还有
「转储数据」sheet（7-8 月，**7,324 条**，占总量的 31%）。

进入「分仓计划监控」页时会触发 `ensureInventoryExtra`，此时内存里的转储数据
会从 16,523 涨到 23,790，说明这两批数据确实是"互补且零重叠"（v202 的设计意图）。

也就是说：**先进转储页看到 16,523 条，先进分仓计划监控再回转储页就看到 23,790 条**——
同一个页面，数据不一致。

修复只需在 `renderTransship` 里加一行触发 `ensureInventoryExtra()`。
但这是**口径问题**（转储页到底该不该含 7-8 月），按你的规则我没有擅自改。
要改的话说一声。
